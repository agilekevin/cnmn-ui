# Portkey AI Gateway (required for AI features)
PORTKEY_API_KEY=your-portkey-api-key

# Provider slugs from Portkey Model Catalog
# Add your provider keys in the Portkey dashboard, then put the slug here.
# Only providers with a slug configured will appear in the model dropdown.
PORTKEY_SLUG_ANTHROPIC=
PORTKEY_SLUG_OPENAI=
PORTKEY_SLUG_GOOGLE=

# Basic Auth (optional - for deployed instances)
# If both are set, users must authenticate to access the app
AUTH_USERNAME=
AUTH_PASSWORD=
